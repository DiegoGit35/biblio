import 'package:flutter/material.dart';

class AltaLibro extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('something nigga'),
    );
  }
}
