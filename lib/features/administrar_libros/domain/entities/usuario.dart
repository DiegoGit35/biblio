class Usuario {
  late String dni, direccion, telefono, nombre, apellido, email;
  String? fechaBaja, codUsuario;

  Usuario(this.apellido, this.direccion, this.dni, this.nombre, this.telefono,
      this.email);
}
